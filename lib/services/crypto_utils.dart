import 'dart:typed_data';

/// Standard AES-256-CBC implementation in pure Dart with zero external dependencies.
/// Complies with FIPS-197 and PKCS#7 standard.
class Aes256Cbc {
  static const int blockSize = 16;
  static const int keySize = 32;
  static const int rounds = 14; // AES-256 requires 14 rounds

  // Forward S-box
  static const List<int> _sBox = [
    
  ];

  // Inverse S-box
  static const List<int> _invSBox = [
    
  ];

  static const List<int> _rcon = [
    0x00, 0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1b, 0x36
  ];

  static int _gMul(int a, int b) {
    int p = 0;
    for (int i = 0; i < 8; i++) {
      if ((b & 1) != 0) p ^= a;
      bool hi = (a & 0x80) != 0;
      a = (a << 1) & 0xff;
      if (hi) a ^= 0x1b;
      b >>= 1;
    }
    return p;
  }

  /// Expand 32-byte key into 240 bytes (60 32-bit words) for 14 rounds
  static Uint32List _expandKey(Uint8List key) {
    if (key.length != keySize) {
      throw ArgumentError('AES-256 key must be exactly 32 bytes.');
    }

    final w = Uint32List(60);
    final byteData = ByteData.sublistView(key);
    for (int i = 0; i < 8; i++) {
      w[i] = byteData.getUint32(i * 4, Endian.big);
    }

    for (int i = 8; i < 60; i++) {
      int temp = w[i - 1];
      if (i % 8 == 0) {
        // RotWord
        temp = ((temp << 8) | (temp >>> 24)) & 0xffffffff;
        // SubWord
        temp = (_sBox[(temp >>> 24) & 0xff] << 24) |
               (_sBox[(temp >>> 16) & 0xff] << 16) |
               (_sBox[(temp >>> 8) & 0xff] << 8) |
               _sBox[temp & 0xff];
        temp ^= (_rcon[i ~/ 8] << 24);
      } else if (i % 8 == 4) {
        // SubWord only
        temp = (_sBox[(temp >>> 24) & 0xff] << 24) |
               (_sBox[(temp >>> 16) & 0xff] << 16) |
               (_sBox[(temp >>> 8) & 0xff] << 8) |
               _sBox[temp & 0xff];
      }
      w[i] = (w[i - 8] ^ temp) & 0xffffffff;
    }

    return w;
  }

  static void _encryptBlock(Uint8List block, Uint32List w, Uint8List out) {
    var state = List<int>.from(block);

    void addRoundKey(int round) {
      for (int c = 0; c < 4; c++) {
        final word = w[round * 4 + c];
        state[c * 4 + 0] ^= (word >>> 24) & 0xff;
        state[c * 4 + 1] ^= (word >>> 16) & 0xff;
        state[c * 4 + 2] ^= (word >>> 8) & 0xff;
        state[c * 4 + 3] ^= word & 0xff;
      }
    }

    void subBytes() {
      for (int i = 0; i < 16; i++) {
        state[i] = _sBox[state[i]];
      }
    }

    void shiftRows() {
      final t = List<int>.from(state);
      state[1] = t[5]; state[5] = t[9]; state[9] = t[13]; state[13] = t[1];
      state[2] = t[10]; state[6] = t[14]; state[10] = t[2]; state[14] = t[6];
      state[3] = t[15]; state[7] = t[3]; state[11] = t[7]; state[15] = t[11];
    }

    void mixColumns() {
      for (int c = 0; c < 4; c++) {
        final i = c * 4;
        final a0 = state[i], a1 = state[i + 1], a2 = state[i + 2], a3 = state[i + 3];
        state[i + 0] = _gMul(0x02, a0) ^ _gMul(0x03, a1) ^ a2 ^ a3;
        state[i + 1] = a0 ^ _gMul(0x02, a1) ^ _gMul(0x03, a2) ^ a3;
        state[i + 2] = a0 ^ a1 ^ _gMul(0x02, a2) ^ _gMul(0x03, a3);
        state[i + 3] = _gMul(0x03, a0) ^ a1 ^ a2 ^ _gMul(0x02, a3);
      }
    }

    addRoundKey(0);
    for (int r = 1; r < rounds; r++) {
      subBytes();
      shiftRows();
      mixColumns();
      addRoundKey(r);
    }
    subBytes();
    shiftRows();
    addRoundKey(rounds);

    for (int i = 0; i < 16; i++) {
      out[i] = state[i];
    }
  }

  static void _decryptBlock(Uint8List block, Uint32List w, Uint8List out) {
    var state = List<int>.from(block);

    void addRoundKey(int round) {
      for (int c = 0; c < 4; c++) {
        final word = w[round * 4 + c];
        state[c * 4 + 0] ^= (word >>> 24) & 0xff;
        state[c * 4 + 1] ^= (word >>> 16) & 0xff;
        state[c * 4 + 2] ^= (word >>> 8) & 0xff;
        state[c * 4 + 3] ^= word & 0xff;
      }
    }

    void invSubBytes() {
      for (int i = 0; i < 16; i++) {
        state[i] = _invSBox[state[i]];
      }
    }

    void invShiftRows() {
      final t = List<int>.from(state);
      state[1] = t[13]; state[5] = t[1]; state[9] = t[5]; state[13] = t[9];
      state[2] = t[10]; state[6] = t[14]; state[10] = t[2]; state[14] = t[6];
      state[3] = t[7]; state[7] = t[11]; state[11] = t[15]; state[15] = t[3];
    }

    void invMixColumns() {
      for (int c = 0; c < 4; c++) {
        final i = c * 4;
        final a0 = state[i], a1 = state[i + 1], a2 = state[i + 2], a3 = state[i + 3];
        state[i + 0] = _gMul(0x0e, a0) ^ _gMul(0x0b, a1) ^ _gMul(0x0d, a2) ^ _gMul(0x09, a3);
        state[i + 1] = _gMul(0x09, a0) ^ _gMul(0x0e, a1) ^ _gMul(0x0b, a2) ^ _gMul(0x0d, a3);
        state[i + 2] = _gMul(0x0d, a0) ^ _gMul(0x09, a1) ^ _gMul(0x0e, a2) ^ _gMul(0x0b, a3);
        state[i + 3] = _gMul(0x0b, a0) ^ _gMul(0x0d, a1) ^ _gMul(0x09, a2) ^ _gMul(0x0e, a3);
      }
    }

    addRoundKey(rounds);
    for (int r = rounds - 1; r >= 1; r--) {
      invShiftRows();
      invSubBytes();
      addRoundKey(r);
      invMixColumns();
    }
    invShiftRows();
    invSubBytes();
    addRoundKey(0);

    for (int i = 0; i < 16; i++) {
      out[i] = state[i];
    }
  }

  /// Encrypts plaintext using AES-256-CBC with PKCS7 padding.
  /// Returns raw ciphertext bytes.
  static Uint8List encrypt({
    required Uint8List plainBytes,
    required Uint8List key,
    required Uint8List iv,
  }) {
    if (key.length != keySize) throw ArgumentError('Key must be 32 bytes.');
    if (iv.length != blockSize) throw ArgumentError('IV must be 16 bytes.');

    final expandedKey = _expandKey(key);

    // PKCS#7 padding
    final padLength = blockSize - (plainBytes.length % blockSize);
    final padded = Uint8List(plainBytes.length + padLength);
    padded.setRange(0, plainBytes.length, plainBytes);
    for (int i = plainBytes.length; i < padded.length; i++) {
      padded[i] = padLength;
    }

    final output = Uint8List(padded.length);
    var prevBlock = Uint8List.fromList(iv);
    final currentBlock = Uint8List(blockSize);
    final encryptedBlock = Uint8List(blockSize);

    for (int offset = 0; offset < padded.length; offset += blockSize) {
      for (int i = 0; i < blockSize; i++) {
        currentBlock[i] = padded[offset + i] ^ prevBlock[i];
      }
      _encryptBlock(currentBlock, expandedKey, encryptedBlock);
      output.setRange(offset, offset + blockSize, encryptedBlock);
      prevBlock = Uint8List.fromList(encryptedBlock);
    }

    return output;
  }

  /// Decrypts ciphertext using AES-256-CBC and strips PKCS7 padding.
  static Uint8List decrypt({
    required Uint8List cipherBytes,
    required Uint8List key,
    required Uint8List iv,
  }) {
    if (key.length != keySize) throw ArgumentError('Key must be 32 bytes.');
    if (iv.length != blockSize) throw ArgumentError('IV must be 16 bytes.');
    if (cipherBytes.isEmpty || cipherBytes.length % blockSize != 0) {
      throw FormatException('Ciphertext length must be a multiple of 16.');
    }

    final expandedKey = _expandKey(key);
    final output = Uint8List(cipherBytes.length);
    var prevBlock = Uint8List.fromList(iv);
    final currentBlock = Uint8List(blockSize);
    final decryptedBlock = Uint8List(blockSize);

    for (int offset = 0; offset < cipherBytes.length; offset += blockSize) {
      currentBlock.setRange(0, blockSize, cipherBytes.sublist(offset, offset + blockSize));
      _decryptBlock(currentBlock, expandedKey, decryptedBlock);
      for (int i = 0; i < blockSize; i++) {
        output[offset + i] = decryptedBlock[i] ^ prevBlock[i];
      }
      prevBlock = Uint8List.fromList(currentBlock);
    }

    // Strip PKCS#7 padding
    final padLength = output.last;
    if (padLength < 1 || padLength > blockSize) {
      throw FormatException('Invalid PKCS#7 padding value: $padLength');
    }
    for (int i = output.length - padLength; i < output.length; i++) {
      if (output[i] != padLength) {
        throw FormatException('Inconsistent PKCS#7 padding');
      }
    }

    return output.sublist(0, output.length - padLength);
  }
}
